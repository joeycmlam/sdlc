# Copilot Agent UI Configuration

# Backend API URL (optional - defaults to http://localhost:8000)
# Set this if your Python backend is running on a different host/port
NEXT_PUBLIC_API_URL=http://localhost:8000
