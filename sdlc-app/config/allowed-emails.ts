/**
 * Permitted Google login emails.
 * Add or remove addresses here to control who can access the app.
 */
export const ALLOWED_EMAILS: ReadonlySet<string> = new Set([
  "joey.cm.lam@gmail.com",
]);
